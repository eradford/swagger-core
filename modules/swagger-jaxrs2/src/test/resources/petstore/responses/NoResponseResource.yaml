openapi: 3.0.1
paths:
  /noresponse:
    get:
      summary: Find pets
      description: Returns the Pets
      operationId: getPets
      responses:
        default:
          description: default response
